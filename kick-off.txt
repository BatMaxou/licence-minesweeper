La classe fait le lien entre l'utilisateur et l'interface front

BACK

- class Minesweeper:
    - OK - init()
    - OK - fillGrid()
    - OK - placeBomb(nb) => nb varie selon la difficulté choisie
    - OK - placeNumbers()
    - OK - countBomb()
    - OK - try(x,y) => return bool selon si bomb ou pas (if nbBox === 0 => revealBox de celles autour)
    - end(bool = false) => gagné/perdu
    - enableFlag() => lock le mode flag (si on re clic sur un flag, disparition de celui-ci)
    - placeFlag(x,y)
    - removeFlag(x,y)
    - isFinished(?) => peut être idée de compteur comparé avec l'aire de la grid - nbBomb

FRONT

- bibliothèque de fonctions:
    - OK - initGrid(L,l)
    - OK - reveal(x,y) => fin de la logistique du click (relance les clics environant ?? )
    - OK - onCellClick => logistique du click
    - OK - onBtnDifficultyClick() => change l'affichage de la grille
    - OK - onBtnStartClick() => disparition des btn difficulty et affichage de celle selectionner et du btn leave
    - displayBombNumber()
    - counterMoves() => ajouter 1 à chaque clic DE L'UTILISATEUR
    - displayFlag(x,y)
    - onBtnLeaveClick()
    - displayWin()
    - displayFail()



BONUS

utiliser local storage (garder partie en cour || stats)??
ajouter chronomètre
animation de fin
    