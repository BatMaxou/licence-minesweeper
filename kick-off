La classe fait le lien entre l'utilisateur et l'interface front

BACK

- class Minesweeper:
    - init()
    - start(l,L,nbBomb)
    - end(bool = false) => gagné/perdu
    - tryBox(x,y)
    - enableFlag() => lock le mode flag (si on re clic sur un flag, disparition de celui-ci)
    - placeFlag(x,y)
    - removeFlag(x,y)

- bibliothèque de fonctions:
    - placeBomb(nb) => nb varie selon la difficulté choisie
    - fillGrid()
    - try(x,y) => return bool selon si bomb ou pas (if nbBox === 0 => revealBox de celles autour)
    - isFinished(?) => peut être idée de compteur comparé avec l'aire de la grid - nbBomb

FRONT

- bibliothèque de fonctions:
    - initGrid(L,l)
    - displayBombNumber()
    - counterMoves() => ajouter 1 à chaque clic DE L'UTILISATEUR
    - displayFlag(x,y)
    - reveal(x,y) => fin de la logistique du click (relance les clics environant ?? )
    - onCellClick => logistique du click
    - onBtnDifficultyClick() => change l'affichage de la grille
    - onBtnStartClick() => disparition des btn difficulty et affichage de celle selectionner et du btn leave
    - onBtnLeaveClick()
    - displayWin()
    - displayFail()



BONUS

utiliser local storage (garder partie en cour || stats)??
ajouter chronomètre
animation de fin
    